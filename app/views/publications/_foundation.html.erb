<div class="child">
  <%= f.collection_select(:survey_id, Survey.all, :id, :name, { :prompt => true }) %>
  <%= f.hidden_field :_destroy, :class => 'destroy' %>
  <%= remove_child_link 'remove', f %>
</div>
